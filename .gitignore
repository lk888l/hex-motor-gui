/src-tauri/target/
/src-tauri/gen/
**/*.rs.bk
.DS_Store

# frontend (Vite + npm) — no trailing slash: also matches a node_modules
# symlink (used by review worktrees), not only a real directory.
/node_modules
/dist/
npm-debug.log*

# CSV recordings
/logs/
*.csv
